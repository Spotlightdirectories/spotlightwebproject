// Moved to src/components/PartnerNavbar.tsx (2026-08) so it can be
// shared with /partner-legal, which needs the same partner-session-aware
// navbar but a different link set. Re-exported here in case anything
// still imports from this path.
export { default } from "@/components/PartnerNavbar";
