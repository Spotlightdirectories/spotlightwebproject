import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Partner Account | Spotlight Directories",
};

export default function PartnerCreateAccountLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
